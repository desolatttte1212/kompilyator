fn add(int a, int b) = int {
    return a + b;
}

fn multiply(int x, int y) = int {
    return x * y;
}

fn main() = void {
    int result = add(5, 10);
    int product = multiply(add(2, 3), 4);
}