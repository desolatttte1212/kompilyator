fn main() {
    int x = 10;
    int y = 20 @ 30; // Ошибка: @
    bool f = true;
    float bad = 1.2.3; // Ошибка: число (прочитает 1.2, точка, потом .3?)
    if (f & f) {} // Ошибка: &
    if (f | f) {} // Ошибка: |
    int 1bad = 1; // Ошибка: число 1, потом идентификатор bad? Или ошибка символа?
    # \$ % ^ // Ошибки: символы
    return 0;

    /* А вот этот комментарий НЕ ЗАВЕРШЕН и съест всё до конца файла.
       Поэтому все тесты ниже будут проигнорированы.
       Это проверка на незавершенный комментарий.
}