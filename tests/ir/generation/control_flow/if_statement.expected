# IR Program

function test: void ()
test_entry:  # ENTRY
  t0 = GT 1, 0  # 1 > 0
  JUMP_IF t0, L_then0  # if condition
L_else1:
  JUMP L_join2
L_then0:
  STORE [x], 5  # init x
  JUMP L_join2
L_join2:  # JOIN
  RETURN
